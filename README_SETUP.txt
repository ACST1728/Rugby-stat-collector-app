Match Stat Collector — Final Build

Deploy:
1) Upload all files to a GitHub repo.
2) Streamlit Cloud → New app → streamlit_app.py
3) (Optional) Secrets:
   RUGBY_DB_PATH="/mount/data/rugby_stats.db"   # otherwise defaults to ./.streamlit_storage/rugby_stats.db
   DROPBOX_TOKEN="your_dropbox_token_here"
4) First login (if empty DB):
   Username: ACST28
   Password: COYB1527
